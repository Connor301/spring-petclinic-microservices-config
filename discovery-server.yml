server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: false
    hostname: discovery-server.petclinic
  # standalone mode
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://discovery-server.petclinic:8761/eureka/

